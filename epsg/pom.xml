<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
      <groupId>org.locationtech.proj4j</groupId>
      <artifactId>proj4j-modules</artifactId>
      <version>1.3.1-SNAPSHOT</version>
    </parent>

    <groupId>org.locationtech.proj4j</groupId>
    <artifactId>proj4j-epsg</artifactId>
    <packaging>jar</packaging>
    <name>Proj4J EPSG</name>
    <description>Java port of the Proj.4 library for coordinate reprojection</description>

    <properties>
        <proj4j.legal.dir>${project.basedir}/..</proj4j.legal.dir>
    </properties>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>http://www.apache.org/licenses/LICENSE-2.0</url>
        </license>
        <license>
            <name>EPSG database distribution license</name>
            <url>https://raw.githubusercontent.com/locationtech/proj4j/master/LICENSE.EPSG</url>
        </license>
    </licenses>

    <build>
        <plugins>
            <!-- This module is the only one distributing EPSG dataset resources, so it also
                 carries the EPSG Terms of Use alongside the inherited LICENSE and NOTICE.md. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <executions>
                    <execution>
                        <id>copy-epsg-legal-files</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.outputDirectory}/META-INF</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${proj4j.legal.dir}</directory>
                                    <includes>
                                        <include>LICENSE.EPSG</include>
                                    </includes>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <artifactId>maven-jar-plugin</artifactId>
                <configuration>
                    <archive>
                        <manifestEntries>
                            <Automatic-Module-Name>org.locationtech.proj4j.epsg</Automatic-Module-Name>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
